# CPU-сборка torch: pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1
ultralytics>=8.3
easyocr>=1.7
opencv-python-headless>=4.9
pillow>=10
numpy<2.3
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
anthropic>=1.0

# Распознавание номеров: специализированный детектор + OCR, обученный на номерах.
# Читает 10 номеров из 16 на кадрах весовой против 4 у EasyOCR.
fast-plate-ocr[onnx]>=1.1
open-image-models>=0.6
